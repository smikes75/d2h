{
  "identifier": "default",
  "description": "Default capability for D2H",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "shell:default",
    "shell:allow-open"
  ]
}
